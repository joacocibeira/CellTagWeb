# Redis hostname (matches docker-compose service name)
REDIS_HOST=redis

# Path inside the container to the image folder
IMAGE_FOLDER=/data/images

# SQLite DB path inside the container
AUTH_DB_PATH=/data/auth/users.db